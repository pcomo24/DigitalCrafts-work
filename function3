import matplotlib.pyplot as plot

def f(x):
    return x * x

def draw():
    xs = list(range(-100, 100))
    ys = []
    for x in xs:
        ys.append(f(x))
    plot.plot(xs, ys)
    plot.show()

if __name__ == "__main__":
    draw()
